# Render Blueprint - JSON Server Deployment
# See https://render.com/docs/blueprint-spec for more info

services:
  - type: web
    name: ocr-admin-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
